# Core face-recognition stack (dlib-backed) — used by face_encode.py (Step 1)
face_recognition==1.3.0
# numpy <2 required by dlib / face_recognition encodings
numpy>=1.24,<2.0

# Ethereum / Sepolia interaction
web3==7.0.0

# Environment variable loading
python-dotenv==1.0.1
